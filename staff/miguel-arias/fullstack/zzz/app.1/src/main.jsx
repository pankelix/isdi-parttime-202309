import React = require ('react'
import ReactDOM = require ('react-dom/client'
import App = require ('./App.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
